<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Likes">

	<select id="exists" resultType="int" parameterType="map">
	    SELECT COUNT(*) FROM likes WHERE board_id = #{board_id} AND user_id = #{user_id}
	</select>
	
	<insert id="save" parameterType="map">
	    INSERT INTO likes (board_id, user_id) VALUES (#{board_id}, #{user_id})
	</insert>
	
	<delete id="delete" parameterType="map">
	    DELETE FROM likes WHERE board_id = #{board_id} AND user_id = #{user_id}
	</delete>
	
	<select id="countByBoardId" parameterType="long" resultType="int">
    SELECT COUNT(*) FROM likes WHERE board_id = #{boardId}
</select>
</mapper>